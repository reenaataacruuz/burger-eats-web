{
"scripts": {
  "start": "npx cypress open",
  "web": "cross-env AMBIENT=web npx cypress run --headed --spec cypress/e2e/desktop && node tesults-upload.js",
  "mobile": "cross-env AMBIENT=mobile npx cypress run --headed --spec cypress/e2e/mobile && node tesults-upload.js"
},
  "devDependencies": {
    "cross-env": "^7.0.3",
    "cypress": "^13.7.1"
  },
  "dependencies": {
    "cypress-tesults-reporter": "^1.4.1",
    "faker-br": "^0.4.1"
  }
}
